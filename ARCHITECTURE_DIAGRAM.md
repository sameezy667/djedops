# DjedOps Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            PRODUCTION ARCHITECTURE                           │
└─────────────────────────────────────────────────────────────────────────────┘

┌──────────────────┐
│                  │
│   USER BROWSER   │
│                  │
└────────┬─────────┘
         │
         │ 1. Build workflow visually
         │    (React Flow drag & drop)
         │
         ▼
┌──────────────────────────────────────────────────────┐
│          FRONTEND (Vercel)                           │
│  ┌──────────────────────────────────────────────┐   │
│  │  Next.js 14 App                              │   │
│  │  - components/WorkflowBuilder.tsx            │   │
│  │  - lib/weil-sdk-wrapper.ts                   │   │
│  └───────────────┬──────────────────────────────┘   │
│                  │                                   │
│   2. POST /api/deploy                                │
│      { workflow: {...}, metadata: {...} }            │
└──────────────────┼──────────────────────────────────┘
                   │
                   │ HTTP Request
                   │ (CORS: frontend domain allowed)
                   ▼
┌──────────────────────────────────────────────────────┐
│          BACKEND (Render.com)                        │
│  ┌──────────────────────────────────────────────┐   │
│  │  Express.js Server (port 3001)               │   │
│  │                                               │   │
│  │  On Startup:                                 │   │
│  │  1. Create ~/.weilliptic/private_key.wc      │   │
│  │     from WALLET_PRIVATE_KEY env var          │   │
│  │  2. Set WC_PRIVATE_KEY=/path/to/wallet       │   │
│  │  3. Log wallet address for verification      │   │
│  │                                               │   │
│  │  Endpoints:                                  │   │
│  │  - GET /health  (wallet info)                │   │
│  │  - POST /api/deploy  (main endpoint)         │   │
│  │  - GET /api/status/:txHash                   │   │
│  └───────────────┬──────────────────────────────┘   │
│                  │                                   │
│   3. Receive workflow JSON                           │
│      Write to temp file                              │
└──────────────────┼──────────────────────────────────┘
                   │
                   │ 4. Execute command:
                   │    widl-cli deploy \
                   │      --workflow /tmp/workflow.json \
                   │      --coordinator weil1... \
                   │      --rpc https://sentinel.unweil.me
                   ▼
┌──────────────────────────────────────────────────────┐
│          widl-cli Binary                             │
│  ┌──────────────────────────────────────────────┐   │
│  │  Installed via bin/install-widl.sh           │   │
│  │  Location: $HOME/widl/widl-cli               │   │
│  │                                               │   │
│  │  Actions:                                    │   │
│  │  1. Read private key from ~/.weilliptic      │   │
│  │  2. Sign transaction with wallet             │   │
│  │  3. Broadcast to WeilChain RPC               │   │
│  │  4. Return transaction hash                  │   │
│  └───────────────┬──────────────────────────────┘   │
│                  │                                   │
│   5. TX Hash: 0xabc123...                            │
└──────────────────┼──────────────────────────────────┘
                   │
                   │ 6. POST to RPC endpoint
                   │    https://sentinel.unweil.me
                   ▼
┌──────────────────────────────────────────────────────┐
│          WEILCHAIN NETWORK                           │
│  ┌──────────────────────────────────────────────┐   │
│  │  Sentinel Testnet                            │   │
│  │  - Receives signed transaction               │   │
│  │  - Validates signature                       │   │
│  │  - Deploys workflow contract                 │   │
│  │  - Assigns contract address                  │   │
│  │  - Returns confirmation                      │   │
│  └───────────────┬──────────────────────────────┘   │
│                  │                                   │
│   7. Contract deployed at: weil1xyz...               │
│      TX confirmed on chain                           │
└──────────────────┼──────────────────────────────────┘
                   │
                   │ 8. widl-cli parses output
                   │    Returns to backend
                   ▼
┌──────────────────────────────────────────────────────┐
│          BACKEND (Render.com)                        │
│  ┌──────────────────────────────────────────────┐   │
│  │  Express Response:                           │   │
│  │  {                                            │   │
│  │    "success": true,                          │   │
│  │    "txHash": "0xabc123...",                  │   │
│  │    "workflowId": "workflow-123",             │   │
│  │    "contractAddress": "weil1xyz..."          │   │
│  │  }                                            │   │
│  └───────────────┬──────────────────────────────┘   │
│                  │                                   │
│   9. JSON response to frontend                       │
└──────────────────┼──────────────────────────────────┘
                   │
                   │ HTTP 200 OK
                   │ { success: true, txHash: ... }
                   ▼
┌──────────────────────────────────────────────────────┐
│          FRONTEND (Vercel)                           │
│  ┌──────────────────────────────────────────────┐   │
│  │  WorkflowBuilder.tsx                         │   │
│  │  - Receives response                         │   │
│  │  - Shows success alert                       │   │
│  │  - Displays TX hash                          │   │
│  │  - Links to block explorer                   │   │
│  └───────────────┬──────────────────────────────┘   │
│                  │                                   │
│   10. "✅ Deployed! TX: 0xabc123..."                 │
└──────────────────┼──────────────────────────────────┘
                   │
                   ▼
         ┌──────────────────┐
         │                  │
         │   USER BROWSER   │
         │  (Success alert) │
         │                  │
         └──────────────────┘
```

---

## Environment Variables Flow

```
┌─────────────────────────────────────────────────────┐
│                 RENDER.COM DASHBOARD                │
│                                                     │
│  Environment Variables (13 total):                 │
│  ┌──────────────────────────────────────────────┐  │
│  │ WALLET_PRIVATE_KEY=47f5f20b...               │  │
│  │ WALLET_ADDRESS=d12e90e9c...                  │  │
│  │ COORDINATOR_CONTRACT_ADDRESS=weil1...        │  │
│  │ WC_PRIVATE_KEY=/path/to/wallet               │  │
│  │ WC_PATH=/path/to/wallet                      │  │
│  │ ... (8 more)                                 │  │
│  └──────────────────┬───────────────────────────┘  │
│                     │                               │
│   Set via Render UI │                               │
└─────────────────────┼───────────────────────────────┘
                      │
                      │ 1. Injected into container
                      │    at runtime
                      ▼
           ┌──────────────────────┐
           │  Backend Server      │
           │  (server.js startup) │
           └──────────┬───────────┘
                      │
                      │ 2. ensureWalletFile() function
                      │    reads WALLET_PRIVATE_KEY
                      ▼
           ┌──────────────────────┐
           │  File System         │
           │  ~/.weilliptic/      │
           │  private_key.wc      │
           └──────────┬───────────┘
                      │
                      │ 3. widl-cli reads from file
                      │    (not environment variable)
                      ▼
           ┌──────────────────────┐
           │  widl-cli Binary     │
           │  Signs transactions  │
           └──────────────────────┘
```

---

## Render Build Process

```
┌─────────────────────────────────────────────────────┐
│                  GITHUB PUSH                        │
│  $ git push origin main                             │
└──────────────────┬──────────────────────────────────┘
                   │
                   │ Webhook triggers Render
                   ▼
┌─────────────────────────────────────────────────────┐
│              RENDER BUILD PROCESS                   │
│                                                     │
│  Step 1: Clone repository                          │
│  ────────────────────────────────────────────────  │
│  $ git clone https://github.com/you/djedops        │
│  $ cd backend/                                      │
│                                                     │
│  Step 2: Install dependencies                      │
│  ────────────────────────────────────────────────  │
│  $ npm ci --production                              │
│    ├─ Triggers preinstall script                   │
│    │  └─ bash ./bin/install-widl.sh                │
│    │     ├─ curl GitHub API                        │
│    │     ├─ Download widl-cli binary               │
│    │     ├─ Extract to $HOME/widl/                 │
│    │     ├─ chmod +x widl-cli                      │
│    │     └─ Test with --help                       │
│    │                                                │
│    ├─ Install express                              │
│    ├─ Install cors                                 │
│    ├─ Install dotenv                               │
│    └─ Install express-rate-limit                   │
│                                                     │
│  Step 3: Start server                              │
│  ────────────────────────────────────────────────  │
│  $ npm start                                        │
│    └─ node server.js                               │
│       ├─ ensureWalletFile()                        │
│       ├─ Initialize Express                        │
│       ├─ Configure middleware                      │
│       └─ Listen on port 3001                       │
│                                                     │
│  ✅ DEPLOYMENT SUCCESSFUL                          │
│  URL: https://djedops-backend.onrender.com         │
└─────────────────────────────────────────────────────┘
```

---

## Local Development Flow

```
Developer Machine (Windows)
────────────────────────────

Terminal 1: Backend Server
┌────────────────────────────────┐
│ PS> cd backend                 │
│ PS> npm start                  │
│                                │
│ Output:                        │
│ [Startup] ✓ Wallet key exists  │
│ ✓ Backend running on :3001     │
└────────────────────────────────┘

Terminal 2: Frontend Dev Server
┌────────────────────────────────┐
│ PS> npm run dev                │
│                                │
│ Output:                        │
│ ✓ Ready on http://localhost:3000 │
└────────────────────────────────┘

Terminal 3: Testing
┌────────────────────────────────┐
│ PS> cd backend                 │
│ PS> .\test-deploy.ps1          │
│                                │
│ Output:                        │
│ {                              │
│   "success": true,             │
│   "txHash": "0xmock..."        │
│ }                              │
└────────────────────────────────┘

Browser: http://localhost:3000/workflows
┌────────────────────────────────┐
│  Workflow Builder              │
│  [Trigger] ──> [Action]        │
│                                │
│  [Deploy to WeilChain] ◄─ Click│
│                                │
│  ✅ Deployed! TX: 0xabc...     │
└────────────────────────────────┘
```

---

## Key Differences: Old vs New Architecture

### ❌ OLD (WSL-based, complex):
```
User → Frontend → CLI Modal → User copies commands
                              ↓
                         User installs WSL
                              ↓
                         User installs widl-cli in WSL
                              ↓
                         User configures wallet
                              ↓
                         User pastes commands in WSL
                              ↓
                         User copies TX hash back
```
**Problems**: Complex setup, manual steps, WSL required

### ✅ NEW (Backend-based, seamless):
```
User → Frontend → Backend API → widl-cli → WeilChain
                     ↓
              Returns TX hash automatically
```
**Benefits**: One-click deployment, no WSL, no manual setup

---

## Security Model

```
Secrets Storage:
┌─────────────────────────────────────────────────────┐
│ 🚫 NEVER COMMIT:                                    │
│  - .env (in .gitignore)                             │
│  - .env.local (in .gitignore)                       │
│  - backend/.env (in .gitignore)                     │
│  - ~/.weilliptic/ directory (created at runtime)    │
│                                                     │
│ ✅ SAFE TO COMMIT:                                  │
│  - .env.example (template only, no real values)    │
│  - Code with process.env.VARIABLE_NAME             │
│  - Documentation with placeholder values           │
└─────────────────────────────────────────────────────┘

Production Secrets (Render):
┌─────────────────────────────────────────────────────┐
│  Environment Variables → Encrypted at rest          │
│                       → Injected at runtime         │
│                       → Never logged                │
│                       → Only accessible to app      │
└─────────────────────────────────────────────────────┘

Wallet Key Flow:
┌─────────────────────────────────────────────────────┐
│  Render Env Var (WALLET_PRIVATE_KEY)                │
│         ↓ (read once at startup)                    │
│  In-Memory Process (server.js)                      │
│         ↓ (write to file, chmod 600)                │
│  File System (~/.weilliptic/private_key.wc)         │
│         ↓ (read by widl-cli only)                   │
│  widl-cli Signs Transaction                         │
│         ↓ (broadcast)                               │
│  WeilChain Network                                  │
└─────────────────────────────────────────────────────┘
```

---

This architecture ensures:
- ✅ No WSL needed for end users
- ✅ One-click deployment experience
- ✅ Secure wallet management
- ✅ Easy hosting on Render/Vercel
- ✅ Fully automated workflow
- ✅ Production-ready error handling
